const getSpeakers = require('./getSpeakers');
const generateToken = require('./generateToken');
const addSpeaker = require('./addSpeaker');
const getSpeaker = require('./getSpeaker');
const removeSpeaker = require('./removeSpeaker');

module.exports = { getSpeakers, generateToken, addSpeaker, getSpeaker, removeSpeaker };
